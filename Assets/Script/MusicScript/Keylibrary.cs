
using UnityEngine;
[CreateAssetMenu (menuName ="BKdata/KeyLibrary")] 

public class Keylibrary : ScriptableObject
{
    public AudioClip Keysound1;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
